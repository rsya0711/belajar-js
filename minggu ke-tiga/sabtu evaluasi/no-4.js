function sistem() {

    //console.log(user);(ini di dipanggil dulu. karna var, dia bisa di panggil tetapi nilai nya undefined )//

    var user = "Ari";

    console.log (user);

    if (true) {
        let score = 90; 
        console.log (score);
    }

    // function tampil() {
    //     // console.log(score); //(tidak bisa dipanggil, karena sudah beda scope)
    // }
    // tampil();
}
sistem();